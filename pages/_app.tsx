import { AppProps } from 'next/app';
import Script from "next/script";

-  Component,
-  pageProps: { session, ...pageProps }
-}) {
  Component,
  pageProps: { session, ...pageProps }
}: AppProps) {
-    <Component {...pageProps} />
-  );
    <Component {...pageProps} />
  );
